# Rust (compiler, cdb, constraint-server, cli)
/target
**/target
Cargo.lock
*.rs.bk

# Claw build artifacts
*.claw.o
/dist

# code-as-database local stores
*.cdb
*.sqlite
*.sqlite-*

# model / corpus (large, not in git)
/model/corpus/data
/model/finetune/checkpoints
*.gguf
*.safetensors

# benchmark run outputs (archived elsewhere)
/bench/results

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# env
.env
.env.local

/dist
model/claw-0.5b-q8.gguf
model/claw-infer
train/adapter-e4b.tar.gz
train/claw-lora-e4b/
