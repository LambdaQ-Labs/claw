{
  "id": "gen-add100-v2",
  "category": "contract",
  "prompt": "Write `add100` : Nat -> Nat using only in-scope `Nat.add`; parameter p0 is x, combine it with 100.",
  "scope": [
    {
      "name": "Nat.add",
      "ty": "Nat, Nat -> Nat"
    }
  ],
  "params": [
    {
      "name": "x"
    }
  ],
  "grade": {
    "compile": true,
    "requires": [],
    "contracts": [
      "result == x + 100",
      "result >= x"
    ],
    "forbidden": [
      "hallucinated-symbol"
    ]
  }
}