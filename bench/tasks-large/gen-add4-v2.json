{
  "id": "gen-add4-v2",
  "category": "contract",
  "prompt": "Write `add4` : Nat -> Nat using only in-scope `Nat.add`; parameter p0 is x, combine it with 4.",
  "scope": [
    {
      "name": "Nat.add",
      "ty": "Nat, Nat -> Nat"
    }
  ],
  "params": [
    {
      "name": "x"
    }
  ],
  "grade": {
    "compile": true,
    "requires": [],
    "contracts": [
      "result == x + 4",
      "result >= x"
    ],
    "forbidden": [
      "hallucinated-symbol"
    ]
  }
}