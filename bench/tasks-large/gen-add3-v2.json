{
  "id": "gen-add3-v2",
  "category": "contract",
  "prompt": "Write `add3` : Nat -> Nat using only in-scope `Nat.add`; parameter p0 is x, combine it with 3.",
  "scope": [
    {
      "name": "Nat.add",
      "ty": "Nat, Nat -> Nat"
    }
  ],
  "params": [
    {
      "name": "x"
    }
  ],
  "grade": {
    "compile": true,
    "requires": [],
    "contracts": [
      "result == x + 3",
      "result >= x"
    ],
    "forbidden": [
      "hallucinated-symbol"
    ]
  }
}