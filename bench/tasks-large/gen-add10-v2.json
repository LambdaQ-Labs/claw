{
  "id": "gen-add10-v2",
  "category": "contract",
  "prompt": "Write `add10` : Nat -> Nat using only in-scope `Nat.add`; parameter p0 is x, combine it with 10.",
  "scope": [
    {
      "name": "Nat.add",
      "ty": "Nat, Nat -> Nat"
    }
  ],
  "params": [
    {
      "name": "x"
    }
  ],
  "grade": {
    "compile": true,
    "requires": [],
    "contracts": [
      "result == x + 10",
      "result >= x"
    ],
    "forbidden": [
      "hallucinated-symbol"
    ]
  }
}