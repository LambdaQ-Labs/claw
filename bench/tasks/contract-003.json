{
  "id": "contract-003",
  "category": "contract",
  "prompt": "Implement `clampTo : Nat, Nat, Nat -> Nat` with parameters (value, lo, hi) named p0, p1, p2 in that order, satisfying: result is within [lo, hi] and equals value when value already in range. Assume lo <= hi. Use only in-scope symbols.",
  "scope": [
    { "name": "Nat.max", "ty": "Nat, Nat -> Nat" },
    { "name": "Nat.min", "ty": "Nat, Nat -> Nat" }
  ],
  "params": [
    { "name": "value" },
    { "name": "lo" },
    { "name": "hi" }
  ],
  "grade": {
    "compile": true,
    "requires": ["lo <= hi"],
    "contracts": ["result >= lo", "result <= hi"],
    "forbidden": ["hallucinated-symbol"]
  },
  "reference": "solutions/contract-003.claw"
}
