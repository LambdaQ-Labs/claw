{
 "id": "ho-py-relu",
 "category": "translate",
 "prompt": "Port this Python to Claw:\n\ndef relu(x, threshold):\n    return max(x - threshold, 0)\n\nTarget `relu` : Nat, Nat -> Nat (p0 = x, p1 = threshold). Nat subtraction already saturates at zero, and there is no zero literal needed if you rely on that.",
 "scope": [
  {
   "name": "Nat.add",
   "ty": "Nat, Nat -> Nat"
  },
  {
   "name": "Nat.sub",
   "ty": "Nat, Nat -> Nat"
  },
  {
   "name": "Nat.mul",
   "ty": "Nat, Nat -> Nat"
  },
  {
   "name": "Nat.max",
   "ty": "Nat, Nat -> Nat"
  },
  {
   "name": "Nat.min",
   "ty": "Nat, Nat -> Nat"
  }
 ],
 "params": [
  {
   "name": "x"
  },
  {
   "name": "threshold"
  }
 ],
 "grade": {
  "compile": true,
  "requires": [],
  "contracts": [],
  "forbidden": [
   "hallucinated-symbol",
   "effect-unsound"
  ]
 }
}